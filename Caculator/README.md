## Projects

### Calculator
A terminal-based calculator that takes two numbers and an operator as input, then performs the corresponding arithmetic operation.

**Concepts practiced:** functions, switch statements, type conversion, string templates, terminal input via `readline-sync`

**To run:**
cd calculator
node calculator.js



## Setup
This repo uses `readline-sync` for terminal-based user input. Install dependencies once at the root:  npm install
